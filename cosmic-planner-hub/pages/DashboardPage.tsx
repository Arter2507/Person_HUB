import React from 'react';
import Dashboard from '../components/dashboard_management/Dashboard';

/**
 * DashboardPage
 * This is the route-level component for the dashboard.
 * Its primary role is to render the main Dashboard layout and logic component.
 */
const DashboardPage: React.FC = () => {
  return <Dashboard />;
};

export default DashboardPage;